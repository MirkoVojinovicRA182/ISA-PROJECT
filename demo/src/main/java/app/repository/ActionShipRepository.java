package app.repository;

import app.domain.ActionShip;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ActionShipRepository extends JpaRepository<ActionShip, Integer> {
}
